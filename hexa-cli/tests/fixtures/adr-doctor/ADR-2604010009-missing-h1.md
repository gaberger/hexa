**Status:** Accepted
**Date:** 2026-04-09
**Drivers:** No H1 title at all — just the frontmatter fields. The detector must flag this because downstream renderers (the dashboard, `hexa adr list`) all key on the title.

## Context

Files without H1 cannot display in the ADR list table. They must be flagged so a human can supply a title.
