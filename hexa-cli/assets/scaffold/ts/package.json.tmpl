{
  "name": "{{name}}",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc && node -e \"const{globSync}=require('node:fs');if(globSync('dist/**/*.test.js').length===0){console.error('No compiled test files under dist/. A gate that runs nothing is not a gate.');process.exit(1)}\" && node --test \"dist/**/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.4"
  }
}
