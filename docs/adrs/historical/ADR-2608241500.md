# ADR-2608241500: Decision text not carried into this repository

---
id: ADR-2608241500
status: historical
date: 2026-09-15
supersedes: []
superseded_by: null
depends_on: []
components: []
---

**Status:** Historical
**Date:** 2026-09-15
**Drivers:** Cited 29 time(s) in this repository; the ADR itself predates the 2026-09-12 history and was not carried over (ADR-2609151930 §2).

## What the code says

- `hexa-infer/src/endpoint.rs:4` — //! ADR-2608241500 P2.4, with one change that matters: the secret is no longer
- `hexa-infer/src/adapters/anthropic.rs:4` — //! ADR-2608241500 P2.3. Prompt caching (`cache_control`), extended thinking
- `hexa-infer/src/adapters/openai_compat.rs:4` — //! ADR-2608241500 P2.3. The HTTP body construction, the Anthropic↔OpenAI
- `hexa-exec/src/tools/memory_search.rs:18` — //! ADR-2608241500 — which made reading a lesson depend on a database.
- `hexa-exec/src/tools/memory_search.rs:84` — // Local memory (ADR-2608241500). This was a GET to the daemon's
- `hexa-core/src/lib.rs:14` — //! Trimmed from 8,257 lines by ADR-2608241500 P7. What went: the state,
- `hexa-cli/tests/aios_compliance.rs:8` — //! `pause`/`resume`/`override` were removed with the daemon (ADR-2608241500
- `hexa-cli/src/commands/analyze.rs:299` — // Full tree-sitter boundary analysis, in-process (ADR-2608241500 P6.2).
- `hexa-cli/src/commands/analyze.rs:457` — // Architectural-health detectors (ADR-2608241500 P6.5). Folded in from the
- `hexa-cli/src/commands/analyze.rs:1960` — // Full tree-sitter analysis, in-process (ADR-2608241500 P6.2).
- `hexa-cli/src/commands/bench.rs:4` — //! evidence-gated direct executor, in-process (ADR-2608241500 P6.2 — it used
- `hexa-cli/src/commands/build.rs:3` — //! Renamed from `hexa swarm build` / `hexa swarm review` (ADR-2608241500 P6.4).
- `hexa-cli/src/commands/ci.rs:52` — /// the other variant. There is only this one now (ADR-2608241500); the verb
- `hexa-cli/src/commands/ci.rs:80` — // against three suites in the daemon. The daemon went in ADR-2608241500 and
- `hexa-cli/src/commands/ci.rs:163` — // In-process (ADR-2608241500 P6.2). This asked the daemon for
- `hexa-cli/src/commands/graph.rs:3` — //! In-process over the `hexa-graph` engine (ADR-2608241500 P6.2). Every verb
- `hexa-cli/src/commands/hey.rs:689` — /// no Claude session was present. None of those exist (ADR-2608241500).
- `hexa-cli/src/commands/inference.rs:446` — // Write the registry (ADR-2608241500 P6.2). This used to POST
- `hexa-cli/src/commands/inference.rs:723` — // The registry file (ADR-2608241500 P6.2). The daemon served these from
- `hexa-cli/src/commands/inference.rs:960` — // Persist the score to the registry file (ADR-2608241500 P6.2).
- `hexa-cli/src/commands/refresh.rs:68` — // and the daemon statusline they configured (ADR-2608241500). Re-syncing a
- `hexa-cli/src/commands/skill.rs:3` — //! In-process (ADR-2608241500 P6.2). It used to ask the daemon for `/api/skills`
- `hexa-cli/src/commands/test.rs:5` — //! and MCP-parity suites went with the daemon they tested (ADR-2608241500).
- `hexa-cli/src/commands/memory/mod.rs:4` — //! (ADR-2608241500). It was the `hexflo_memory` SpacetimeDB table, reached
- `hexa-cli/src/commands/bootstrap/validate.rs:75` — // are deleted (ADR-2608241500); an inference backend is the only
- `hexa-analysis/src/lib.rs:19` — // (ADR-2608241500 P6.5). One analysis crate, one entry point — the separate
- `hexa-analysis/src/analyzers/mod.rs:3` — //! Folded in from the `hexa-analyzer` crate (ADR-2608241500 P6.5), whose
- `docs/analysis/2609151900-adrs-as-memory-investigation.md:32` — (38), ADR-2608241500 (27), ADR-2026-04-27-0800 (25), ADR-047 (16). `CODEOWNERS`
- `docs/analysis/2609151900-adrs-as-memory-investigation.md:111` — removed in ADR-2608241500. An agent mid-task cannot ask what is already known.

## Decision

Unknown. This stub exists so the citation resolves. Replace it with a real ADR if the decision is rediscovered, or rewrite the citations to the ADR that now governs.

## Consequences

The citing sites resolve to this file instead of to nothing.

## Implementation

None; this stub records citations only.

## References

ADR-2609151930 §2, which requires every cited id to resolve to a file.
