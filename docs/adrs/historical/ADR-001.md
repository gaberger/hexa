# ADR-001: Decision text not carried into this repository

---
id: ADR-001
status: historical
date: 2026-09-15
supersedes: []
superseded_by: null
depends_on: []
components: []
---

**Status:** Historical
**Date:** 2026-09-15
**Drivers:** Cited 55 time(s) in this repository; the ADR itself predates the 2026-09-12 history and was not carried over (ADR-2609151930 §2).

## What the code says

- `clippy.toml:2` — # Pairs with ADR-001-hexagonal-architecture.md and
- `clippy.toml:6` — # Hex import rules (canonical — see CLAUDE.md / ADR-001):
- `hexa-cli/src/commands/docs.rs:624` — assert!(has_yaml_frontmatter("---\nid: ADR-001\n---\n# title"));
- `hexa-cli/src/commands/docs.rs:627` — assert!(!has_yaml_frontmatter("---\nid: ADR-001\n"));
- `hexa-cli/src/commands/docs.rs:632` — let content = "# ADR-001\n**Status:** Accepted\n**Date:** 2026-03-15\n";
- `hexa-cli/src/commands/adr/doctor.rs:464` — // Pre-Date-field-convention ADRs (ADR-001 onward) all hit this.
- `hexa-cli/src/commands/adr/doctor.rs:1821` — let f = finding("ADR-001", PathBuf::from("x.md"), FindingKind::UnparseableStatus, "bad");
- `hexa-cli/src/commands/adr/doctor.rs:1824` — assert_eq!(f.adr_id, "ADR-001");
- `hexa-cli/src/commands/adr/doctor.rs:1836` — let warn = finding("ADR-001", PathBuf::from("x.md"), FindingKind::StaleProposed, "");
- `hexa-cli/src/commands/adr/doctor.rs:1843` — let err = finding("ADR-001", PathBuf::from("x.md"), FindingKind::DuplicateId, "");
- `hexa-cli/src/commands/adr/doctor.rs:1850` — let err  = finding("ADR-001", PathBuf::from("x.md"), FindingKind::DuplicateId, "");
- `hexa-cli/src/commands/adr/doctor.rs:1857` — let err  = finding("ADR-001", PathBuf::from("x.md"), FindingKind::DuplicateId, "");
- `hexa-cli/src/commands/adr/doctor.rs:1897` — assert!(!lenient_has_status_line("# ADR-001\n\nNo status here"));
- `hexa-cli/src/commands/adr/doctor.rs:1920` — let content = "## Context\n\nReferences ADR-001 in prose.\n\n**Depends on:** ADR-002\n";
- `hexa-cli/src/commands/adr/doctor.rs:2311` — "ADR-001",
- `hexa-cli/src/commands/adr/doctor.rs:2325` — "ADR-001",
- `hexa-cli/src/commands/adr/doctor.rs:2472` — "ADR-001",
- `hexa-cli/src/commands/adr/doctor.rs:2529` — let f = finding("ADR-001", PathBuf::from("x.md"), FindingKind::DuplicateId, "");
- `hexa-cli/src/commands/adr/doctor.rs:2543` — let f = finding("ADR-001", PathBuf::from("x.md"), FindingKind::DuplicateId, "");
- `hexa-cli/src/commands/adr/doctor.rs:2554` — "ADR-001",
- `hexa-cli/src/commands/adr/doctor.rs:2575` — "ADR-001",
- `hexa-cli/src/commands/adr/doctor.rs:2602` — let text = "see ADR-001 and ADR-2026-04-15-0100 (§2), also ADR-2609121400; not ADR- alone\nADR-2026-05-09 too";
- `hexa-cli/src/commands/adr/doctor.rs:2604` — assert_eq!(ids, vec!["ADR-001", "ADR-2026-04-15-0100", "ADR-2609121400", "ADR-2026-05-09"]);
- `hexa-cli/src/commands/adr/doctor.rs:2617` — (PathBuf::from("hexa-exec/src/x.rs"), 7, "ADR-001".to_string()),
- `hexa-cli/src/commands/adr/doctor.rs:2619` — (PathBuf::from("CODEOWNERS"), 1, "ADR-001".to_string()),
- `hexa-cli/src/commands/adr/doctor.rs:2625` — assert!(f.iter().all(|x| x.adr_id == "ADR-001"));
- `hexa-cli/src/commands/adr/doctor.rs:2632` — (PathBuf::from("hexa-exec/src/x.rs"), 7, "// per ADR-001 the loop commits on green".to_string()),
- `hexa-cli/src/commands/adr/doctor.rs:2633` — (PathBuf::from("docs/EVIDENCE.md"), 40, "ADR-001 made this a gate".to_string()),
- `hexa-cli/src/commands/adr/doctor.rs:2635` — let s = orphan_stub("ADR-001", &sites);
- `hexa-cli/src/commands/adr/doctor.rs:2636` — assert!(s.starts_with("# ADR-001: "), "H1 carries the id");
- `hexa-cli/src/commands/adr/doctor.rs:2646` — let sites = vec![(PathBuf::from("a.rs"), 1, "ADR-001 x".to_string())];
- `hexa-cli/src/commands/adr/doctor.rs:2647` — let written = write_orphan_stubs(d.path(), &[("ADR-001".to_string(), sites.clone())]);
- `hexa-cli/src/commands/adr/doctor.rs:2649` — let p = d.path().join("historical").join("ADR-001.md");
- `hexa-cli/src/commands/adr/doctor.rs:2652` — let again = write_orphan_stubs(d.path(), &[("ADR-001".to_string(), sites)]);
- `hexa-cli/src/commands/adr/mod.rs:1601` — assert_eq!(parse_adr_status("# ADR-001\n\n**Status:** Accepted\n**Date:** 2026-01-01\n"), "accepted");
- `hexa-cli/src/commands/adr/mod.rs:1611` — assert_eq!(parse_adr_status("# ADR-001: No status here\n\nJust text.\n"), "unknown");
- `hexa-cli/src/commands/adr/mod.rs:1617` — assert_eq!(parse_adr_status("# ADR-001\n\n**Status**: Accepted\n"), "unknown");
- `hexa-cli/src/commands/adr/mod.rs:1623` — assert_eq!(parse_adr_status("# ADR-001\n\n- **Status**: Accepted\n"), "unknown");
- `hexa-cli/src/commands/adr/mod.rs:1629` — assert_eq!(parse_adr_status("# ADR-001\n\n## Status\nAccepted\n"), "accepted");
- `hexa-cli/src/commands/adr/mod.rs:1636` — parse_adr_status("# ADR-001\n\n## Status\n**Accepted** | Open\n"),
- `hexa-cli/src/commands/adr/mod.rs:1645` — parse_adr_status("# ADR-001\n\n## Status\n**Accepted** — 2026-04-10\n"),
- `hexa-cli/src/commands/adr/mod.rs:1653` — assert_eq!(parse_adr_status("# ADR-001\n\n## Status\n\nProposed\n"), "proposed");
- `hexa-cli/src/commands/adr/mod.rs:1659` — assert_eq!(parse_adr_status("# ADR-001\n\n## Status:\nAccepted\n"), "accepted");
- `hexa-cli/src/commands/adr/mod.rs:1669` — let path = std::path::Path::new("ADR-001-test.md");
- `hexa-cli/src/commands/adr/mod.rs:1670` — assert_eq!(extract_title(path, "# ADR-001: My Title\n"), "ADR-001: My Title");
- `hexa-cli/src/commands/adr/mod.rs:1670` — assert_eq!(extract_title(path, "# ADR-001: My Title\n"), "ADR-001: My Title");
- `hexa-cli/src/commands/adr/mod.rs:1675` — let path = std::path::Path::new("ADR-001-test.md");
- `hexa-cli/src/commands/adr/mod.rs:1676` — assert_eq!(extract_title(path, "No heading here\n"), "ADR-001-test");
- `hexa-cli/src/commands/adr/mod.rs:1704` — assert_eq!(extract_adr_id("adr-001-foo"), "ADR-001");
- `hexa-cli/src/commands/adr/mod.rs:1768` — assert_eq!(adr_date("ADR-001", c).as_deref(), Some("2026-03-15"));
- `hexa-cli/src/commands/adr/mod.rs:1786` — parse_superseded_by("**Superseded by:** ADR-001\n").as_deref(),
- `hexa-cli/src/commands/adr/mod.rs:1787` — Some("ADR-001")
- `hexa-analysis/src/fingerprint_extractor.rs:609` — let adr = "# ADR-001\n\n## Context\n\nSome context.\n\n## Decision\n\nWe will use hexagonal architecture. More details follow.\n\n## Consequences\n\n...";
- `hexa-analysis/src/analyzers/composition_churn.rs:394` — let bold = "# ADR-001\n\n**Status:** Accepted\n**Date:** 2026-04-01\n\nbody\n";
- `docs/analysis/2609151900-adrs-as-memory-investigation.md:31` — The most-cited decisions are all missing: ADR-001 (39 citations), ADR-2026-04-15-0100

## Decision

Unknown. This stub exists so the citation resolves. Replace it with a real ADR if the decision is rediscovered, or rewrite the citations to the ADR that now governs.

## Consequences

The citing sites resolve to this file instead of to nothing.

## Implementation

None; this stub records citations only.

## References

ADR-2609151930 §2, which requires every cited id to resolve to a file.
