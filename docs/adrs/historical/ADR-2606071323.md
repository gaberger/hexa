# ADR-2606071323: Decision text not carried into this repository

---
id: ADR-2606071323
status: historical
date: 2026-09-15
supersedes: []
superseded_by: null
depends_on: []
components: []
---

**Status:** Historical
**Date:** 2026-09-15
**Drivers:** Cited 14 time(s) in this repository; the ADR itself predates the 2026-09-12 history and was not carried over (ADR-2609151930 §2).

## What the code says

- `hexa-exec/src/adversarial.rs:661` — /// (ADR-2606071323 §4) so autonomous commits are attributable and never masquerade as
- `hexa-exec/src/direct_exec.rs:51` — /// checked-out branch (ADR-2606071323). Defaults to TRUE (safe-by-default):
- `hexa-exec/src/direct_exec.rs:479` — /// Should this run be isolated to its own worktree? Default TRUE (ADR-2606071323)
- `hexa-exec/src/direct_exec.rs:503` — // ADR-2606071323: confine the run to its own worktree unless the operator
- `hexa-exec/src/direct_exec.rs:1046` — // (ADR-2606071323 §4) so they are attributable and never masquerade as the
- `hexa-exec/src/direct_react.rs:79` — // ADR-2606071323: confine the run to its own worktree unless the operator
- `hexa-exec/src/direct_react.rs:104` — /// The ReAct loop body, run inside the resolved workspace (ADR-2606071323).
- `hexa-exec/src/direct_workspace.rs:1` — //! Per-run git-worktree isolation for the autonomous executor (ADR-2606071323).
- `hexa-exec/src/direct_workspace.rs:14` — //! operator's to manage (ADR-2606071323 scope).
- `hexa-exec/src/direct_workspace.rs:85` — /// P5 guard (ADR-2606071323): an isolated run must NEVER resolve to the
- `hexa-exec/src/lib.rs:5` — //! ADR-2606071323), transcript compression, the tool-use protocol
- `hexa-cli/src/commands/bench.rs:14` — //! (ADR-2606071323), which forks a `hexa/auto/<slug>` worktree as a sibling of the repo and
- `hexa-cli/src/commands/direct.rs:49` — // (ADR-2606071323 scopes `hexa do` out of worktree isolation — the
- `hexa-cli/src/commands/init.rs:460` — /// identity (ADR-2606071323 §4: attributable, never masquerades as the operator).

## Decision

Unknown. This stub exists so the citation resolves. Replace it with a real ADR if the decision is rediscovered, or rewrite the citations to the ADR that now governs.

## Consequences

The citing sites resolve to this file instead of to nothing.

## Implementation

None; this stub records citations only.

## References

ADR-2609151930 §2, which requires every cited id to resolve to a file.
