# ADR-2606071340: Decision text not carried into this repository

---
id: ADR-2606071340
status: historical
date: 2026-09-15
supersedes: []
superseded_by: null
depends_on: []
components: []
---

**Status:** Historical
**Date:** 2026-09-15
**Drivers:** Cited 5 time(s) in this repository; the ADR itself predates the 2026-09-12 history and was not carried over (ADR-2609151930 §2).

## What the code says

- `hexa-git/src/lib.rs:3` — //! it is reusable outside hexa-nexus (ADR-2606071340 P1). Daemon-coupled adapters
- `hexa-exec/src/lib.rs:1` — //! hexa-exec — the single-agent execution loop (ADR-2606071340 P1).
- `hexa-core/Cargo.toml:23` — # (ADR-2606071340 P1). No async runtime is pulled into hexa-core.
- `hexa-core/src/ports/mod.rs:5` — // rest (ADR-2606071340 P1). Implemented by the STDB/SQLite adapters.
- `hexa-cli/src/commands/graph.rs:48` — /// Who depends on a module/file — the excision-safety oracle (ADR-2606071340).

## Decision

Unknown. This stub exists so the citation resolves. Replace it with a real ADR if the decision is rediscovered, or rewrite the citations to the ADR that now governs.

## Consequences

The citing sites resolve to this file instead of to nothing.

## Implementation

None; this stub records citations only.

## References

ADR-2609151930 §2, which requires every cited id to resolve to a file.
