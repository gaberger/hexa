# ADR-050: Decision text not carried into this repository

---
id: ADR-050
status: historical
date: 2026-09-15
supersedes: []
superseded_by: null
depends_on: []
components: []
---

**Status:** Historical
**Date:** 2026-09-15
**Drivers:** Cited 11 time(s) in this repository; the ADR itself predates the 2026-09-12 history and was not carried over (ADR-2609151930 §2).

## What the code says

- `hexa-cli/src/commands/plan/mod.rs:34` — /// ADR reference (e.g. ADR-050). Required unless --no-adr is set.
- `hexa-cli/src/commands/plan/mod.rs:1280` — // ADR-050: Validate ADR reference exists before creating workplan
- `hexa-cli/src/commands/hook/mod.rs:12` — //! ADR-050: Hook-Enforced Agent Lifecycle Pipeline
- `hexa-cli/src/commands/hook/mod.rs:25` — /// Extended session state file (ADR-050).
- `hexa-cli/src/commands/hook/mod.rs:315` — // ADR-050: the active workplan, from local memory.
- `hexa-cli/src/commands/hook/mod.rs:630` — // ADR-050: Validate file falls within workplan adapter boundary
- `hexa-cli/src/commands/hook/mod.rs:720` — // ADR-050: Detect destructive operations
- `hexa-cli/src/commands/hook/mod.rs:1100` — /// ADR-050: Check if file being edited falls within the workplan's declared adapter boundary.
- `hexa-cli/src/commands/hook/mod.rs:1223` — // ── ADR-050: Lifecycle helpers ───────────────────────────────────────
- `hexa-cli/src/commands/hook/mod.rs:1253` — /// Load the active workplan from local memory into session state (ADR-050).
- `hexa-cli/src/commands/hook/mod.rs:1274` — /// Detect destructive bash commands (ADR-050).

## Decision

Unknown. This stub exists so the citation resolves. Replace it with a real ADR if the decision is rediscovered, or rewrite the citations to the ADR that now governs.

## Consequences

The citing sites resolve to this file instead of to nothing.

## Implementation

None; this stub records citations only.

## References

ADR-2609151930 §2, which requires every cited id to resolve to a file.
