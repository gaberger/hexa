# ADR-047: Decision text not carried into this repository

---
id: ADR-047
status: historical
date: 2026-09-15
supersedes: []
superseded_by: null
depends_on: []
components: []
---

**Status:** Historical
**Date:** 2026-09-15
**Drivers:** Cited 19 time(s) in this repository; the ADR itself predates the 2026-09-12 history and was not carried over (ADR-2609151930 §2).

## What the code says

- `hexa-cli/src/main.rs:225` — /// Internal documentation health (ADR-047) — terminology, freshness, module READMEs
- `hexa-cli/src/commands/docs.rs:1` — //! `hexa docs` — internal-documentation health (ADR-047 Phase 4).
- `hexa-cli/src/commands/docs.rs:5` — //!   - stale terminology (canonical glossary from ADR-047 §"Glossary"),
- `hexa-cli/src/commands/docs.rs:36` — /// Add YAML frontmatter to one ADR (ADR-047 Phase 5). Idempotent; skips ADRs
- `hexa-cli/src/commands/docs.rs:39` — /// ADR ID (e.g. `ADR-047`) or path to an ADR file.
- `hexa-cli/src/commands/docs.rs:63` — // ─── Canonical glossary (ADR-047) ──────────────────────────────────────────
- `hexa-cli/src/commands/docs.rs:75` — println!("{} Canonical glossary terms (ADR-047)", "\u{2b21}".cyan());
- `hexa-cli/src/commands/docs.rs:88` — "  {} Source: docs/adrs/ADR-047-internal-documentation-system.md",
- `hexa-cli/src/commands/docs.rs:182` — println!("{} Documentation health (ADR-047)", "\u{2b21}".cyan());
- `hexa-cli/src/commands/docs.rs:268` — "ADR has no YAML frontmatter (ADR-047 Phase 5). Run `hexa docs migrate-adr <id>`."
- `hexa-cli/src/commands/docs.rs:456` — //   legacy:    ADR-047               → consume digits only
- `hexa-cli/src/commands/docs.rs:648` — let path = std::path::Path::new("ADR-047-foo.md");
- `hexa-cli/src/commands/docs.rs:649` — let content = "# ADR-047: Foo\n\n**Status:** Accepted\n**Date:** 2026-03-22\n";
- `hexa-cli/src/commands/docs.rs:653` — assert!(fm.contains("id: ADR-047"));
- `hexa-cli/src/commands/docs.rs:671` — let p = std::path::Path::new("docs/adrs/ADR-047-internal-docs.md");
- `hexa-cli/src/commands/docs.rs:672` — assert_eq!(adr_id_from_filename(p), "ADR-047");
- `hexa-cli/src/commands/adr/doctor.rs:2609` — let hits = cited_adr_ids("a\nb ADR-047\nc");
- `hexa-cli/src/commands/adr/doctor.rs:2610` — assert_eq!(hits, vec![(2, "ADR-047".to_string())]);
- `docs/analysis/2609151900-adrs-as-memory-investigation.md:32` — (38), ADR-2608241500 (27), ADR-2026-04-27-0800 (25), ADR-047 (16). `CODEOWNERS`

## Decision

Unknown. This stub exists so the citation resolves. Replace it with a real ADR if the decision is rediscovered, or rewrite the citations to the ADR that now governs.

## Consequences

The citing sites resolve to this file instead of to nothing.

## Implementation

None; this stub records citations only.

## References

ADR-2609151930 §2, which requires every cited id to resolve to a file.
