name: hexa test pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  lint:
    name: Lint (clippy + tsc)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-lint-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-lint-

      - name: Clippy (workspace)
        # Deny only correctness + suspicious — see ci.yml for the
        # rationale on dropping the blanket `-D warnings` policy.
        run: cargo clippy --workspace --exclude hexa-desktop -- -D clippy::correctness -D clippy::suspicious

      - name: Clippy (spacetime-modules)
        run: cd spacetime-modules && cargo clippy --workspace -- -D clippy::correctness -D clippy::suspicious

      - name: Install bun
        uses: oven-sh/setup-bun@v2

      - name: TypeScript type check (skip if src/ removed)
        run: |
          if [ -d src ] && ls src/**/*.ts 1>/dev/null 2>&1; then
            bun run check
          else
            echo "No TypeScript sources in src/ — skipping"
          fi

  unit:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-test-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-test-

      - name: Build dashboard assets (required by hexa-nexus rust-embed)
        run: |
          cd hexa-nexus/assets && npm install && npm run build || mkdir -p ../assets/dist

      - name: Rust workspace tests
        run: cargo test --workspace --exclude hexa-desktop

      - name: SpacetimeDB module tests
        run: cd spacetime-modules && cargo test --workspace

      - name: Install bun
        uses: oven-sh/setup-bun@v2

      - name: TypeScript tests (skip if no test files)
        run: |
          if ls tests/unit/*.test.ts 1>/dev/null 2>&1 || ls tests/smoke/*.test.ts 1>/dev/null 2>&1; then
            bun test tests/unit/ tests/smoke/
          else
            echo "No TypeScript test files found — skipping"
          fi

      - name: Install dashboard deps
        run: cd hexa-nexus/assets && npm ci --ignore-scripts
        continue-on-error: true

      - name: Dashboard store tests (Vitest)
        run: cd hexa-nexus/assets && npx vitest run
        continue-on-error: true

  integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: [lint, unit]
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-integ-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-integ-

      - name: Ensure dashboard assets directory exists
        run: mkdir -p hexa-nexus/assets/dist

      - name: Build hexa-nexus
        run: cargo build -p hexa-nexus --release

      - name: Build hexa-cli
        run: cargo build -p hexa-cli --release

      - name: Start hexa-nexus (SQLite fallback — no SpacetimeDB in CI)
        run: |
          ./target/release/hexa-nexus &
          sleep 3
        env:
          HEXA_STATE_BACKEND: sqlite

      - name: Service health tests
        run: ./target/release/hexa test services

      - name: Architecture checks
        run: ./target/release/hexa test arch
        continue-on-error: true

      - name: Stop hexa-nexus
        if: always()
        run: pkill hexa-nexus || true

  e2e:
    name: E2E Browser Tests
    runs-on: ubuntu-latest
    needs: [integration]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-e2e-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-e2e-

      - name: Install agent-browser
        run: npm install -g @anthropic/agent-browser
        continue-on-error: true

      - name: Build and run E2E
        run: |
          mkdir -p hexa-nexus/assets/dist
          cargo build -p hexa-nexus --release
          cargo build -p hexa-cli --release
          chmod +x scripts/e2e-test.sh
          ./scripts/e2e-test.sh
        continue-on-error: true
        env:
          HEXA_STATE_BACKEND: sqlite

      - name: Upload E2E screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-screenshots
          path: tests/e2e/*.png
          if-no-files-found: ignore
