#!/usr/bin/env bash
# hexa-intf pre-commit hook — runs security gate before every commit
# Install: git config core.hooksPath .githooks

exec "$(git rev-parse --show-toplevel)/scripts/security-check.sh"
