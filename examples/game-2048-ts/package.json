{
  "name": "game-2048-ts",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "rm -rf dist && tsc && node --test dist/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
